service: () -> {
    claimGift: (text, text, text, text) -> (text);
    createGift: (text, text, text, text) -> (record {tokenId:text; tokenAddress:text; createdAt:nat64; message:text; receiver:text});
    prepareTx: () -> (text);
    publicKey: () -> (record {publicKeyString:text; publicKey:vec nat8; address:text; addressFromEthers:text});
    transactionCount: (text) -> (nat);
}
